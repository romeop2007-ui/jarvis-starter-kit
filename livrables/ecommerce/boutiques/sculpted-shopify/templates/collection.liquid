{%- comment -%}
  Zooryn — Page collection. Utilise le layout 'sculpted' (header + footer + CSS partagés),
  donc rendu identique au reste de la boutique. Affiche la grille via sculpted-collection.
  S'applique à TOUTES les collections. Pour personnaliser le titre/sous-titre, édite la
  section "Zooryn — Collection" dans Personnaliser.
{%- endcomment -%}
{% layout 'sculpted' %}
{% section 'sculpted-collection' %}
