{%- comment -%}
  Sculpted — Guide des tailles. L'ancre #size correspond au lien "Size guide" de la buy box.
  Une seule image 1254×1254 (le guide complet, tableau inclus dans l'image).
{%- endcomment -%}
<section class="size" id="size">
  <div class="wrap">
    <div class="frame size-single{% if section.settings.image != blank %} has-img{% endif %}">
      {%- if section.settings.image != blank -%}
        <img src="{{ section.settings.image | image_url: width: 1254 }}" alt="{{ section.settings.image_alt | escape }}" width="1254" height="1254" loading="lazy">
      {%- else -%}
        <span>1254 × 1254</span>
      {%- endif -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Sculpted — Size guide",
  "tag": "section",
  "settings": [
    { "type": "image_picker", "id": "image", "label": "Image (1254 × 1254 px)" },
    { "type": "text", "id": "image_alt", "label": "Texte alternatif (invisible, SEO)", "default": "Sculpted size guide" }
  ]
}
{% endschema %}
